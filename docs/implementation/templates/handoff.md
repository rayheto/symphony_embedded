# 任务交付记录

- task_id / owner / issue_id：
- source_revision / artifact_revision：
- 关联R与TC：
- 实际修改路径和行为：
- 接口/状态/数据兼容影响：
- 遇到的问题与解决过程（Case/Evidence引用）：
- 运行命令、exit_code、报告/原始材料：
- 结果：passed / failed / skipped / blocked / not_run（逐项填写）：
- 已知限制和后续依赖：
- 下一接手者所需上下文：

本记录用于Agent交接，默认不要求人类逐条签字。需要人的方向性决定另列具体问题、选项、证据与影响。

Git状态：base branch/SHA、任务分支、已验证tree、commit SHA、push结果、PR URL/base/head与Draft/Ready状态；未获合并授权时明确awaiting_review，不写merged。
