# M5 视觉验收（TC17/TC18）— 证据与状态

状态：**五页原生截图 + 两张补拍已逐张读图评审**，评审结果见「三、逐张评审」与「五、TC17 逐条对照」。
TC17 的感知部分从 `not_run` 变为**已评审**，但按下面的对照表，仍有若干条只能记为部分或无法判定，
理由都写在该行里——不是「看起来没问题所以通过」。

评审方式与边界：

- 评审者是模型读图：逐张看 1440×900 的原生 PNG（含按需裁切的局部），**并用像素度量交叉核对**，
  不靠感觉下结论；
- 页面文案与字段**以模板和 HTTP 响应为准**，不从 PNG 里 OCR 抠业务数据（ACCEPTANCE 的失败/反向检查明文禁止）；
- tokens、字号、圆角、焦点样式**以 `priv/static/workbench.css` 为准**，再用渲染图核对是否真的落到了像素上；
- 每一步都指得到证据：像素统计、元素包围盒、对比度数值、源码行号。

## 一、这一批判定所依据的图

| 产物 | 路径 | 尺寸 | 字节 |
|---|---|---|---|
| 看板 | `docs/verification/evidence/screenshots/issues.png` | 1440×900 | 100214 |
| 设备 | 同目录 `devices.png` | 1440×900 | 49615 |
| 设备 · 串口页签（补拍） | 同目录 `devices-serial.png` | 1440×900 | 94726 |
| 审阅记录 | 同目录 `reviews.png` | 1440×900 | 50361 |
| 项目架构 | 同目录 `architecture.png` | 1440×900 | 250871 |
| Issue · 调查与证据（补拍） | 同目录 `issue-evidence.png` | 1440×900 | 93407 |
| 运行状态（工作台之前的页） | 同目录 `dashboard.png` | 1440×900 | 87375 |
| 架构产物侧影 + contact sheet | `docs/architecture/symphony-embedded-workbench/*.png` | — | — |

`manifest.txt` 里后两行（`devices-serial`、`issue-evidence`）是**手工对已配置实例取的**，理由见「三、2/6」；
脚本默认那一跑只会写五页基线，重跑一次会把这两行冲掉，所以补拍的复现命令写在「十一、」。

## 二、先说结论：这一批图证明了什么

- **八卡首屏**：看板 1440×900 首屏恰好 8 张白卡（按列扫白底区：3+3+1+1，卡片宽 310px），
  与「8 个 Issue · 3 个任务运行中 · 1 项待审阅」对得上。
- **Soft Glass tokens 真的落到了像素上**：五页里出现的主色**精确等于** token 十六进制值
  （`#F5F5F7`/`#FFFFFF`/`#EEEEF2`/`#DEDEE3`/`#1D1D1F`/`#686870`/`#0066CC`/`#FFF4DB`），
  没有离板底色、没有旧 14px 灰调、没有大侧栏。
- **双行导航**：五页都是两行，每行 44px，中间 1px `#DEDEE3` 分隔；导航底色是
  `rgba(255,255,255,.88)` 与画布合成出的 **#FEFEFE**（不是纯白），确认「只有导航是轻磨砂」。
- **每区域至多一个蓝色主动作**：看板 1 个（新建 Issue，104×40）；设备串口 1 个（开始采集，93×40）；
  审阅 0 个；架构页首屏 0 个；运行状态 0 个。没有一处出现两个蓝。
- **正文对比度**：`#1D1D1F`/`#F5F5F7` = **15.46:1**，次要文字 `#686870`/画布 = **5.07:1**，
  均过 4.5:1。

## 三、逐张评审

### 1. `issues.png` — 看板

- 结构：品牌 + 工作空间名 → 第二行导航（Issues/项目架构/设备/审阅记录，Issues 高亮为浅灰胶囊 71×36）
  → 标题行（`28px` 标题：实测墨高 27px，与 28px 字号一致）→ 工具行（筛选输入 + 应用 + **灰色分段控件**
  看板/列表 113×36 + 单个蓝色「新建 Issue」）→ 四列看板（待办 3 / 进行中 3 / 待审阅 / 已完成）。
- 状态表达：每列标题前是圆点 + **列名文字**，颜色不是唯一载体（TC17 反向检查通过）。
- 卡片：白底、16px 圆角、阴影 + 1px 内环；卡内依次是 Issue key（等宽）、标题、标签胶囊、负责人与相对时间。
- 留白：内容到 y=792（视口 88%），底部留白正常，没有溢出。
- 结论：**这一页的 TC17 条目可以判通过**。唯一没有出现的是日志区（该页当前不渲染日志）。

### 2. `devices.png` — 设备（未选中任何设备）

- 结构：标题「设备」+「2 台设备」+ 刷新（次级按钮）+ 提示「选择一台设备查看采集与观测」
  → 一张 1392×190 的白面板，里面是设备表：设备 / 连接 / 构建 / 占用，两行
  （开发板 A（STM32F4）· rev A · unknown · build-17；开发板 B（未接线）· rev B · unknown · 未知）。
- 因为**没有选中设备**，右侧详情面板（含「设备视图」灰色分段控件、开始采集）**没有渲染**，
  页面只占了视口 41%，其余是空画布。
- 该页没有蓝色主动作（刷新是次级样式），与「0 个蓝」的度量一致，不是漏样式。
- 诚实降级：`hardware_revision` 为 `unknown`、占用为「无」——环境里没有真目标板，
  页面照实显示，没有编造。
- 结论：列表部分通过；**TC17 关于「设备 tab 用灰色分段控件」的那条在这一张里看不到**，
  所以补拍了下一张。

### 3. `devices-serial.png`（补拍，`?device=board-a&tab=serial`）

- 这一张才包含要判的东西：**「设备视图」灰色分段控件 114×36**（轨道 `#EEEEF2`、选中项白底 8px 圆角）、
  **唯一的蓝色主动作「开始采集」93×40**、以及「在线只表示连接可用，不代表验证通过……」的琥珀提示条
  （`#946000` on `#FFF4DB` = 4.89:1，颜色之外还有整句话，不靠颜色单独表意）。
- 内容到 y=643（72%），没有溢出。
- 日志区**没有出现**：`<pre class="wb-log">` 带 `:if={@rows != []}`，没有采集会话就没有行，
  于是这张图里没有日志可看。

### 4. `reviews.png` — 审阅记录（空态）

- 实测：内容只到 y=432，全页 7 行文字；面板里是「0 个方案决定 · 0 条审阅意见」，
  下面两块空态文案（「还没有记录到方案决定……」「还没有审阅意见。」）。
- 为什么是空的：这个 data root 的 journal 里**没有任何决定记录**（`projects/*/records.jsonl`
  只有 2 条记录），而没有执行器上报草稿决定就不会有决定——这正是「无 Codex/无目标板」那条阻塞的
  同一个原因，不是页面缺陷。
- 因此「采用方案」这个蓝色主动作**在这一张里不存在**（源码里它有 `disabled={!@selected_option}`，
  且没有决定可选），审阅页的 0 个蓝由此而来。
- 页内 方案决定/审阅意见 的切换用的是 `wb-tabs`（选中项浅灰胶囊），不是 `wb-segmented`；
  这一点我是量出来的（该页只有 84×36 的导航胶囊和 28×28 头像两处 `#EEEEF2` 区域），
  单看图容易看错。
- 结论：**审阅页只评审到空态**；满态（候选方案 / 意见输入 / 采用方案）在这一批图里没有被证据，
  只有测试（`operations_test.exs` "adopting a decision"）覆盖行为。

### 5. `architecture.png` — 项目架构

- 结构：标题「项目架构」+「last-good：symphony-embedded-workbench-source · rev 2」+ 刷新
  → 左侧是**产物 viewer（iframe，图内原样式）**，右上是产物信息（源码图 / 独立打开 /
  已交付修订 2 · 来源 revision `3600812f…`），右下是两块面板：结构对比、组件索引。
- 图里能看到 IR 里的东西：组件（Tracker（Linear）/Orchestrator/AgentRunner/Codex app-server/工程工具/
  Experience.Store/Devices.Manager/SerialPort + helper/目标板/工作台页面）、连线标注
  （轮询 Issue、派发、工具调用、先落盘再回执、读取投影、采集会话、chunk 落盘、原始字节）、
  宿主进程边界框、以及三张图例卡（已实现并验证 / 已验证但受限 / 未验证）。
  换句话说：**宿主页面是 Soft Glass，图内是它自己的 viewer**，符合 TC17 的这一条。
- 诚实降级：组件索引里十个组件全部是 实现=`planned`、验证=`unverified`，产物页自己写着
  「无法判断是否过期」（没有被管理产品的 repo revision 可比）——没有为了好看把状态抬高。
- 溢出/首屏：面板底边被 900px 视口切断（底行 x=24 处仍是白色、看不到圆角），
  也就是 **「登记请求」这个唯一蓝色主动作在首屏之下**，需要滚动。
  这是「首屏内容取整」的取舍结果（viewer 占了约 730px 高），登记它，不当作缺陷。

### 6. `issue-evidence.png`（补拍，`?tab=investigation`）

- 补拍理由：TC17 反向检查里「补证据为真按钮」在 Issue 调查页，五张基线图不含这一页。
- 实测：右侧面板里**一个 103×40 的带边框控件**（`wb-btn`，`phx-click="request-evidence"`），
  旁边是带边框的输入框，外面是琥珀提示框。
- 这一页没有蓝色主动作（这些动作都是次级样式），0 个蓝仍然满足「至多一个」。
- 顺带暴露一个真实观感问题：这批控件是白底 + `#DEDEE3` 边框放在白面板/画布上，
  边框几乎看不见（原因见下面的对比度表），这是本次评审里最值得处理的一条。

### 7. `dashboard.png` — 运行状态（`/`）

- 这是工作台**之前**就存在的页，脚本刻意把它一起拍下来做对照
  （`workbench-shots.sh` 的注释写了这一点）。它有自己的字号体系（rem 级、更大）和自己的绿调，
  单行头部，不是 `wb-` 双行外壳。
- 所以它**不适用 TC17 的四页基线**：TC17 判的是工作台四页 + 架构页。这里只做对照记录，
  不按 Soft Glass 要求它。

## 四、度量证据

### tokens：精确命中（只统计等于 token 十六进制值的像素）

| token | 值 | 出现处（举例） |
|---|---|---|
| canvas | `#F5F5F7` | 五页页面底：看板 377k、设备 609k、审阅 572k、架构 105k、运行状态 217k 像素 |
| surface | `#FFFFFF` | 卡片/面板底 |
| inset | `#EEEEF2` | 激活导航胶囊、分段控件轨道、日志底 |
| border | `#DEDEE3` | 卡片/面板/控件描边；导航两行之间的分隔线 |
| text | `#1D1D1F` | 正文 |
| secondary | `#686870` | 次要文字 |
| accent | `#0066CC` | 唯一主动作、链接 |
| warning / warning-tint | `#946000` / `#FFF4DB` | 演示数据提示条（看板/审阅/Issue 页，1392×47，逐页像素数完全相同） |

### 几何

| 量 | 实测 | 依据 |
|---|---|---|
| 导航行高 | 44px + 44px，中间 1px `#DEDEE3`，整条 88px | 逐行扫 x=4 的颜色序列 |
| 导航磨砂 | 合成色 **#FEFEFE**（`rgba(255,255,255,.88)` 压画布） | 同上；CSS `.wb-nav` 有 `backdrop-filter: blur(16px)` |
| 标题字号 | 28px | CSS `.wb-title`；渲染墨高 27px（CJK 满 em） |
| 正文字号 | 15px/1.5 | CSS `body`；渲染墨高 12–15px |
| 圆角 | 控件 10px / 卡片 16px / 面板 20px | CSS 变量；蓝按钮角弧实测 6–7px（阈值化后的弧长，与 10px 一致） |
| 按钮高 | 40px（`min-height:36px` + 8px 内距 + 1px 边框） | 蓝块包围盒 104×40、93×40 |
| 导航胶囊 | 71×36 / 84×36 / 54×36 | `#EEEEF2` 区域包围盒 |
| 分段控件 | 轨道 113×36（看板）、114×36（设备），内项 32px | 同上 + CSS `.wb-segmented` |
| 头像 | 28×28 圆形 | `#EEEEF2` 区域 |
| 看板卡 | 310px 宽；首屏 8 张 | 四列按列扫白底连续段 |

### 对比度（WCAG 相对亮度，越大越好）

| 组合 | 比值 | 要求 | 判定 |
|---|---|---|---|
| 正文 `#1D1D1F` / 画布 `#F5F5F7` | 15.46:1 | ≥4.5 | 通过 |
| 正文 / 表面 `#FFFFFF` | 16.83:1 | ≥4.5 | 通过 |
| 正文 / 内嵌底 `#EEEEF2` | 14.54:1 | ≥4.5 | 通过 |
| 次要 `#686870` / 画布 | 5.07:1 | ≥4.5 | 通过 |
| 次要 / 表面 | 5.52:1 | ≥4.5 | 通过 |
| 次要 / 内嵌底 | 4.77:1 | ≥4.5 | 通过 |
| 白字 / 品牌蓝 | 5.57:1 | ≥4.5 | 通过 |
| 品牌蓝 / 画布 | 5.11:1 | ≥4.5 | 通过（也是焦点环颜色，`:focus-visible` 2px） |
| 警示字 / 警示底 | 4.89:1 | ≥4.5 | 通过 |
| 成功 / 审阅 / 已完成 各色 / 表面 | 5.35 / 5.51 / 5.98:1 | ≥4.5 | 通过 |
| **边框 `#DEDEE3` / 表面** | **1.34:1** | 3:1（关键边界） | **未达标** |
| **边框 `#DEDEE3` / 画布** | **1.23:1** | 3:1（关键边界） | **未达标** |

### 触控热区

| 控件 | 实测 | 目标 | 判定 |
|---|---|---|---|
| 按钮 | 40px 高 | 36px 按钮 | 通过；44px 触控热区 **未达** |
| 导航链接胶囊 | 36px 高 | 44px | **未达** |
| 分段控件内项 | 32px 高 | 44px | **未达** |
| 头像 | 28×28 | 44px | **未达**（纯展示，`aria-hidden`） |

## 五、TC17 逐条对照

| 通过条件/反向检查 | 判定 | 依据 |
|---|---|---|
| 八卡首屏 | **通过** | 看板首屏实测 8 张白卡 |
| 正文 15px / 标题 28px | **通过** | CSS + 渲染墨高 |
| 10/16/20px 圆角 | **通过** | CSS 变量 + 按钮角弧实测 |
| 每区域至多一个蓝色主动作 | **通过** | 逐页蓝块统计：1/1/0/0/0 |
| 仅导航轻磨砂 | **通过** | 只有 `.wb-nav` 有 `backdrop-filter`；渲染合成色 #FEFEFE |
| 日志不透明（无玻璃叠玻璃） | **源码通过，图不可判** | `.wb-log` 用不透明 `#EEEEF2`、无 `backdrop-filter`；这一批图里日志区因无采集行而未渲染 |
| 双行导航/账号入口/所有字段保留 | **通过** | 五页都是两行；第一行含 查看运行状态/设置/操作者/头像 |
| 架构宿主同风格、图内原 viewer | **通过** | 架构页外壳是 Soft Glass；图在 iframe 内用自己的样式 |
| 设备 tab 用灰色分段控件 | **通过** | `devices-serial.png`：轨道 114×36 `#EEEEF2` |
| 补证据为真按钮 | **通过** | `issue-evidence.png`：103×40 带边框控件 |
| 颜色不能唯一表达状态 | **通过** | 列标题有文字 + 圆点；提示条有整句文案 |
| 正文 4.5:1 | **通过** | 见对比度表（最低 4.77:1） |
| 关键边界 3:1 | **未达标** | 边框 token 只有 1.23–1.34:1 |
| 36px 按钮 / 44px 触控热区 | **部分** | 按钮 40px 达标；导航/分段/头像不足 44px |
| 不从 PNG 取色/OCR 改业务数据 | **遵守** | 文案取自模板与响应；取色只用于验收测量 |
| 不沿用旧 14px/小圆角/深灰主按钮/大侧栏 | **通过** | 未出现；主按钮是 token 蓝 |
| 禁止玻璃叠玻璃/透明日志 | **通过（源码）** | 页面内没有第二处 backdrop-filter |

## 六、TC18：迭代状态、键盘、焦点

这一批图**判不了 TC18**，只能给源码依据，别当成通过：

- 焦点可见：`:focus-visible { outline: 2px solid #0066CC; outline-offset: 3px }`（5.11:1，够亮）；
- 降级媒体查询齐备：`@supports not (backdrop-filter)`、`prefers-reduced-transparency: reduce`
  （导航退回纯白）、`prefers-reduced-motion: reduce`（关掉过渡与动画）；
- 1280/1920 回归有断点：`max-width:1280px` 看板两列、`max-width:900px` 单列；
- 还没做的：键盘顺序、焦点回到哪、hover/按下态、加载/空/错/陈旧态的逐态截图、
  断线不假在线。**这些都还是未验证**。

## 七、看图判定不了、需要别的证据的

| 项 | 为什么这张图不算数 |
|---|---|
| 键盘顺序、焦点可见、hover | 静态图没有交互状态 |
| 审阅页满态（采用方案） | 环境里没有决定记录，只有空态 |
| 设备串口日志 | 没有采集会话就没有行 |
| 真实浏览器里的 iframe 隔离与恶意 HTML | 需要对真实交付的 HTML 做浏览器侧验证（见 `m4-hardening.md` 的 TC15 行） |
| 1280/1920 回归 | 本轮只拍了 1440×900 |

## 八、记下来但没定性为缺陷的两条

1. **边界的对比度口径**：`#DEDEE3` 只有 1.23–1.34:1。卡片/面板靠「白底 + 阴影」而不是靠边框成形，
   但**输入框、次级按钮**是「白底 + 该边框」放在白面板上，边界实际不可见。
   两条路选一条：把边框当作关键边界就加深（例如 `#8E8E93` 在画布与表面上都≈3:1），
   或者写明它不承担「关键边界」职责并给出依据。**我没有改 token**，这是需要 owner 定的口径。
2. **图例文字与投影状态容易读串**：产物图例卡写「已实现并验证」，而同一页的组件索引对十个组件
   投影出 `planned` / `unverified`。两者语义不同（前者是作者写的说明，后者是从 Issue/Validation
   记录投影出的状态），但用词重叠，读者容易以为在说同一件事。`validate/deliver` 只校验 IR 的结构，
   不校验这层语义一致，所以这条是**建议**：要么改图例措辞，要么在页面上注明两者口径不同。

## 九、之前为什么做不了（已修复，留档）

曾记为「Chrome 无法完成任何 http/https 导航」，并把方向指向代理，那是误判。实际原因：

headless Chrome 启动后要向会话 keyring（gnome-keyring，经 D-Bus）索要 cookie 加密密钥。本环境
`XDG_SESSION_TYPE=tty`，该调用不返回，于是 CookieStore 的持久层永远加载不完——NetLog 里只有
`COOKIE_PERSISTENT_STORE_KEY_LOAD_STARTED`，从头到尾没有一次 `..._LOAD_FINISHED`。而
`URLRequestHttpJob` 在 cookie 这一步是同步等待的，结果是**所有带 cookie 的请求（即所有导航）都
永远停在拿到 socket 之前**，且不报任何错。

加 `--password-store=basic` 即恢复。`elixir/scripts/workbench-shots.sh` 与 archify 的
`bin/visual-check.mjs` 都已带上该 flag。

排查中被误读的现象，记下来免得再走一遍：

| 现象 | 实际含义 |
|---|---|
| 服务器侧两次 `ACCEPT` 后 `recv` 返回 0 | 那两个 socket 是 `is_preconnect=true` 的**预连接**，在 +510ms 被 `SOCKET_POOL_CLOSING_SOCKET` 以 `reason="Cert verifier changed"` 清池；**主框架请求从未绑定到 socket** |
| 外网请求也挂住（`example.com`/`neverssl.com`） | 所以与代理、与「本地直连」都无关；走代理的系统请求（`clients2.google.com/time`、组件更新）反而全部成功，因为 `URLRequestHttpJob` 那一步它们不查 cookie |
| `--no-proxy-server`、显式 `--proxy-server`、清空 proxy 环境变量均无效 | 与代理无关的旁证 |

磁盘缓存正常（`HTTP_CACHE_GET_BACKEND`/`OPEN_OR_CREATE_ENTRY` 都完成），`--no-sandbox` 也与本次无关。

### 环境修好后第一次采集是失败的，而且是真的缺陷

`--password-store=basic` 之后第一次 `visual-check` 退出 **1**：1440×900 上
`scrollHeight=937 > 900`（1600×1000 也差 19px），1920 与 2048 通过。也就是说，浏览器能跑之后
测出的第一个事实是**这张图在该视口下装不进首屏**，而不是「环境好了所以通过」。

按上游的修复顺序（先压缩间距、删掉真正冗余的内容，再考虑节点与字号）压缩了 IR 的排版节奏：
行距收紧、边界 `pad` 24→18；没有改节点尺寸、字号或拓扑。重新出图后 `deliver` 9/9，
`visual-check` 退出 0，四个视口的 `scrollHeight` 与 `innerHeight` 相等：

| 视口 | 修复前 scrollHeight | 修复后 |
|---|---|---|
| 1440×900 | 937（溢出 37px） | 900 |
| 1600×1000 | 1019（溢出 19px） | 1000 |
| 1920×1080 | 1080 | 1080 |
| 2048×1320 | 1320 | 1320 |

## 十、没有据此声称的东西

- 没有把「服务器返回 200」当作页面渲染通过。
- 没有用生成的 HTML 反推业务数据或状态。
- 没有从 PNG 取色或 OCR 去改业务数据；取色只用于验收测量。
- 没有降低任何门槛：`make all` 与原有阈值未改动。
- 没有把「我读了图」当作 TC18 通过；也没有把「空态正确」当作满态通过。

## 十一、复现与继续

```sh
elixir/scripts/workbench-shots.sh                 # 默认写到 docs/verification/evidence/screenshots
ARCHIFY_CHROME=/path/to/chrome elixir/scripts/workbench-shots.sh /tmp/shots

# 对着已经在运行的实例取图（例如登记了真实设备清单、发布了真实架构产物的那一个），
# 而不是脚本自己起的临时实例：
SHOT_BASE_URL=http://127.0.0.1:4123 elixir/scripts/workbench-shots.sh
```

补拍的两张（对已配置实例，同一套 Chrome 参数）：

```sh
chrome --headless --disable-gpu --no-sandbox --hide-scrollbars --password-store=basic \
  --window-size=1440,900 --virtual-time-budget=4000 \
  --screenshot=docs/verification/evidence/screenshots/devices-serial.png \
  'http://127.0.0.1:4123/workbench/devices?device=board-a&tab=serial'

chrome … --screenshot=docs/verification/evidence/screenshots/issue-evidence.png \
  'http://127.0.0.1:4123/workbench/issues/EMB-46?tab=investigation'
```

剩下的（按 TC17/TC18 的提交证据清单）：

1. 1280 / 1920 两个宽度的回归图；
2. 各页 loading/empty/error/stale/unsupported/denied 的逐态截图与键盘走查（TC18）；
3. 审阅页满态：需要一条真实的决定记录（有执行器上报草稿决定，或演示路径能给出决定）；
4. 关键边界对比度口径的判定（见「八、1」）；
5. 真实浏览器下的产物隔离与恶意 HTML 验证（TC15）。

若在任何机器上再次遇到「导航无错挂住」，先在 NetLog 里查
`COOKIE_PERSISTENT_STORE_KEY_LOAD_STARTED` 有没有配对的 `..._LOAD_FINISHED`，再查
`SOCKET_POOL_CLOSING_SOCKET` 的 reason——这两条比看代理配置快得多。